/**
 * Data Source Configuration
 * 
 * Set USE_SUPABASE to false to use local demo data instead of Supabase.
 * When false, the app will use demo data from src/lib/demo-data.ts
 * 
 * To disable Supabase, comment out or set to false:
 * export const USE_SUPABASE = false;
 */

export const USE_SUPABASE = false;

